@model IEnumerable<N.Treinamento.Application.ViewModels.RacaViewModel>
@using GL.Treinamento.UI.Site.Helpers;

@{
    ViewBag.Title = "Listagem de raças";
}

<h2>Listagem de raças</h2>

<p>
    @Html.ActionLink("Cadastrar raça", "Create", "Create", new { @class = "btn btn-primary" }).IfClaimHelper("PermissoesCliente", "CI")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomeRaca)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeRaca)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.RacaId }, new { @class = "btn btn-warning" }).IfClaimHelper("PermissoesCliente", "CE")
                @Html.ActionLink("Excluir", "Delete", new { id = item.RacaId }, new { @class = "btn btn-danger" }).IfClaimHelper("PermissoesCliente", "CX")
                @Html.ActionLink("Detalhes", "Details", new { id = item.RacaId }, new { @class = "btn btn-info" }).IfClaimHelper("PermissoesCliente", "CD")
            </td>
        </tr>
    }

</table>
